package io.github.olegnyr.adocmobile

/**
 * Первый шов expect/actual в проекте. Заведён в каркасе намеренно: он доказывает,
 * что механизм платформенных реализаций собран и работает, до того как через него
 * пойдут рендер и превью — два настоящих шва из видения.
 */
expect fun platformName(): String
