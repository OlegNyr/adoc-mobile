= <Название фичи>
:feature: NNN-<slug>
// tier: S | M | L
:tier: M
// status: draft | approved | in-progress | done — управляет плашкой ниже, писать одним словом
:status: draft
:owner: <owner>
:created: ГГГГ-ММ-ДД
:updated: ГГГГ-ММ-ДД
:targets: shared/src/commonMain/kotlin/<пакет>/<фича>/**, shared/src/commonTest/kotlin/<пакет>/<фича>/**
:toc: macro
:toc-title: Содержание
:sectanchors:
:icons: font
// Значки статуса — одним набором на документ. В таблицах ссылайся атрибутом ({st-todo}),
// руками значок не пиши: иначе легенда и таблицы разъезжаются.
:st-todo: ⬜ не начат
:st-wip: 🔄 в работе
:st-done: ✅ закрыт
:st-hold: ⏸️ отложен
:st-drop: ❌ отменён

[cols="1,1,1,1,1", frame=none, grid=none]
|===
| *Фича* {feature} | *Tier* {tier} | *Статус* {status} | *Владелец* {owner} | *Обновлён* {updated}
|===

ifeval::["{status}" == "draft"]
[CAUTION]
.Черновик — писать код по этой спеке нельзя
====
Правило «спека раньше кода»: реализация начинается только после `:status: approved`.
Одобрение — правка этого атрибута владельцем, а не «ок» в чате.
====
endif::[]

ifeval::["{status}" == "approved"]
[TIP]
.Спека одобрена — можно реализовывать
====
Реализация ведётся против этого текста.
Разошлись — правь документ и оставь ссылку в журнале, а не расходись молча.
====
endif::[]

ifeval::["{status}" == "in-progress"]
[NOTE]
.Реализация идёт
====
Статус слайсов — в `plan.adoc`, что уже сделано — в `progress.adoc`.
Журналу работ здесь не место.
====
endif::[]

ifeval::["{status}" == "done"]
[NOTE]
.Фича закрыта
====
Перед переводом в этот статус должна была пройти сверка спеки и кода скиллом `spec-verification`.
====
endif::[]

[NOTE]
====
Заполняй по tier: *S* — разделы 0 и 4; *M* — 0–6 (раздел 2 чеклистом, UC кратко, раздел 5 якорями); *L* — все разделы, включая Design anchors с ADR и полную матрицу.
Проектную специфику (стек, слои, инварианты) бери из `CLAUDE.md` и `doc/development-rules.adoc`.
====

toc::[]

== Видение

Проблема:: <Боль, от лица пользователя, без решения.>
Для кого:: <Роль или сценарий.>
Желаемый результат:: <Как выглядит мир после. Измеримо, если можно.>
Не-цели:: <Что осознанно не делаем.>
Ценность и почему сейчас:: <…>

== Требования

=== Функциональные

[cols="1,3,1,1", options="header"]
|===
| ID | Требование | Приоритет | Источник

| FR-1
| Система ДОЛЖНА <…>
| Must
| Видение

| FR-2
| Пользователь МОЖЕТ <…>
| Should
| <…>
|===

Формулировка — ДОЛЖНА / МОЖЕТ / НЕ ДОЛЖНА; приоритет по MoSCoW (Must, Should, Could, Won't).

=== Нефункциональные

[IMPORTANT]
====
Пройди каждую строку. Неприменимо — так и напиши «неприменимо», а не удаляй строку: пустая строка неотличима от забытой.
Чеклист — проектный (мобильное приложение на Kotlin Multiplatform), а не backend-сервиса.
====

[cols="1,1,3", options="header"]
|===
| ID | Категория | Требование или решение

| NFR-1
| Отзывчивость
| Пороги в числах: подсветка, рендер, реакция на ввод. Из чего следует порог

| NFR-2
| Доля общего кода
| Что уходит в `commonMain`; нужен ли новый `expect`/`actual`-шов (это развилка уровня ADR)

| NFR-3
| Офлайн
| Работает ли без сети; если выходит наружу — куда, что уходит, что при недоступности

| NFR-4
| Паритет платформ
| Совпадает ли поведение Android и iOS; если платформа отстаёт — это решение, а не умолчание

| NFR-5
| Жизненный цикл
| Уход в фон, выгрузка процесса, поворот, возврат: что сохраняется, что восстанавливается

| NFR-6
| Файлы пользователя
| Кодировка, переводы строк, права доступа после перезапуска, поведение при отказе записи

| NFR-7
| Ввод и клавиатура
| IME, автокоррекция, выделение, `WindowInsets.ime`, положение каретки

| NFR-8
| Безопасность и данные
| Секреты в защищённом хранилище; что запрещено писать в логи; что уходит на внешний сервис

| NFR-9
| Доступность
| Цвет не единственный носитель смысла; размер шрифта; контраст мелкого текста

| NFR-10
| Тестируемость
| Что покрывается в `commonTest` без устройства; что требует инструментального прогона и почему
|===

=== Ограничения и допущения

Ограничения:: <версии стека, решения видения, которые нельзя ломать, ограничения песочницы ОС.>
Допущения:: <что считаем истинным.>
Зависимости:: <библиотеки, платформенные API, внешние сервисы, доступы.>

== Интеграция в приложение

Поверхности:: <экраны, точки входа, системные интенты, share sheet — что затрагивается и кто потребитель.>
Общий код:: <куда ложится логика в `commonMain`; какие модели и швы затрагиваются.>
Платформенные адаптеры:: <нужен ли `expect`/`actual`; какой платформенный API; что ломается при его отсутствии.>
Состояние и корутины:: <владелец состояния, диспетчеры, отмена, дебаунсы.>
Настройки:: <что становится настройкой, что меняется без релиза, значение по умолчанию.>

== Сценарии использования

=== UC-1: <Цель актора>

Актор:: <кто или какая система> · *Тип триггера:* `user` | `system-event` · *Уровень:* user-goal | subfunction
Триггер, предусловия, постусловия:: <…>
Основной сценарий:: . <…> . <…>
Расширения:: 2a. <условие → реакция>; *a. <ошибка или отмена → обработка>
Покрывает:: FR-1, FR-2

== Тест-кейсы

[NOTE]
====
Сильные критерии: конкретика вместо оценок, наблюдаемый результат, данные-примеры, один сценарий — один результат, оракул.
Тип: `automatable` (kotlin.test в `commonTest`, корутины через `kotlinx-coroutines-test`, UI — Compose UI test, инструментальные — на устройстве) или `manual`.
====

=== TC-1 (happy, automatable) — покрывает UC-1, FR-1

`[@test]` shared/src/commonTest/kotlin/<пакет>/<Что>Test.kt

[source,gherkin]
----
Given <исходное состояние с конкретными данными>
When  <действие>
Then  <наблюдаемый результат: содержимое файла, список диапазонов, HTML, состояние экрана>
----

Имя теста начинается с идентификатора: `TC-1 …` — по нему сверка находит покрытие.
Если теста ещё нет — `[@test] (нет)`, и тест-кейс закрывается слайсом.

=== TC-2 (edge) · TC-3 (negative) · TC-4 (отказ зависимости или отсутствие сети)

== Технический подход

[NOTE]
Якоря, сужающие «как», а не полный дизайн. Гипотезы, не жёсткий контракт.

Паттерны для подражания:: <классы и фичи-образцы проекта.>
Точки интеграции:: <экраны, швы, модели, платформенные адаптеры.>
Предполагаемые артефакты:: <новые файлы, ресурсы, настройки.>
Явные рамки и запреты:: <чего не делать. Разворачиваются в границы плана.>
Связанные ADR:: ADR-… (крупные развилки — в `adr/`).

== Матрица трассируемости

[cols="1,1,1,1,1", options="header"]
|===
| Требование | Use Case | Test Case | Слайс | Статус

| FR-1
| UC-1
| TC-1
| SL-2
| ⬜
|===

Колонка «Слайс» заполняется на фазе декомпозиции, «Статус» ведётся по `plan.adoc`.
Подробности сделанного — в `progress.adoc`, не здесь.

== Открытые вопросы

* [ ] <что решить до старта реализации>

Только нерешённое.
Как только вопрос закрыт, решение переезжает в требование, ADR или журнал, а отсюда удаляется.
Эта секция — не журнал работ.

== Ссылки

* План работ: `plan.adoc` · Журнал: `progress.adoc`
* Под-спеки (tier L): `spec/<подсистема>.spec.adoc`
* ADR: `adr/adr-001-<slug>.adoc`
* Связанные фичи и разведка: <…>
