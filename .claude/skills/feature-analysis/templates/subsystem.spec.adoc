= <Подсистема>
:name: <Подсистема — человекочитаемое имя>
:description: <Одна фраза: что эта подсистема обязана обеспечивать.>
:feature: NNN-<slug>
:subsystem: S-A
// status: draft | approved | implemented
:status: draft
:targets: shared/src/commonMain/kotlin/<пакет>/<подсистема>/**, shared/src/commonTest/kotlin/<пакет>/<подсистема>/**
:sectanchors:
:icons: font

ifeval::["{status}" == "implemented"]
[NOTE]
.Подсистема реализована
====
Каждое требование ниже должно иметь `[@test]` на существующий тест.
Расхождение ловится скиллом `spec-verification`.
====
endif::[]

[NOTE]
Только для tier *L* и когда `analysis.adoc` перевалил за ~600 строк.
Срез требований по одной подсистеме — чтобы агент держал в контексте нужное, а не всю фичу целиком.
Требования здесь *не дублируют* аналитику, а раскрывают её функциональные требования до уровня контракта.

== Назначение

<Что подсистема делает и чего не делает. Три-пять строк.>

== Требования

* <Требование в форме ДОЛЖНА / МОЖЕТ / НЕ ДОЛЖНА, проверяемое.> — закрывает FR-1 +
`[@test]` shared/src/commonTest/kotlin/<пакет>/<Что>Test.kt
* <…> — закрывает NFR-3 +
`[@test]` shared/src/commonTest/kotlin/<пакет>/<Что>Test.kt

`[@test]` ставится рядом с требованием, которое тест проверяет, — это заменяет ручную колонку матрицы.
Путь должен существовать; если теста ещё нет — пометь `[@test] (нет)` и заведи слайс.

== Контракт

Публичные точки входа:: <функции и типы, которыми пользуются другие подсистемы.>
Что подсистема ожидает снаружи:: <зависимости, инварианты вызывающего.>
Ошибки:: <как сообщает о сбое: исключение, результат-тип, состояние; кто обрабатывает.>

== Потоки, отмена, отказы

<Диспетчеры и потокобезопасность, отмена корутин, поведение при уходе в фон, таймауты внешних вызовов и реакция на их отказ — конкретно для этой подсистемы.
Общий чеклист — в нефункциональных требованиях аналитики.>

== Известные ограничения

* <Что осознанно не покрыто. Со ссылкой на слайс или открытый вопрос аналитики.>
